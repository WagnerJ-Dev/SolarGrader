"""Data acquisition — all free public sources, each in its own module.

    tiles       USGS 3DEP LiDAR tile discovery + download + collection selection
    buildings   building footprints (Microsoft / county / OpenStreetMap)
    irradiance  EU PVGIS typical-meteorological-year data
    regions     county/state → bounding box (US Census TIGERweb)
"""
